
<!--
  This Behavior Tree replans the global path periodically at 1 Hz and it also has
  recovery actions specific to planning / control as well as general system issues.
  This will be continuous if a kinematically valid planner is selected.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">

      <PipelineSequence name="NavigateWithReplanning">
        <!-- RateController: 0.2Hz = 5s interval.
             PHẢI > movement_time_allowance (2.5s) để progress_checker kịp trigger trước khi path bị reset.
             Công thức: replanning_interval > movement_time_allowance -->
        <RateController hz="0.2">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased" server_timeout="5000"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap" server_timeout="5000"/>
          </RecoveryNode>
        </RateController>

        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{path}" controller_id="FollowPath" server_timeout="5000"/>
          <Sequence name="FollowPathRecoveryActions">
            <BackUp backup_dist="0.60" backup_speed="0.3" time_allowance="60.0" server_timeout="5000"/>
            <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap" server_timeout="5000"/>
          </Sequence>
        </RecoveryNode>

      </PipelineSequence>

      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <Sequence name="ClearingActions">
            <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap" server_timeout="5000"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap" server_timeout="5000"/>
          </Sequence>
          <!-- <BackUp name="AckermannTurnAround" server_name="ackermann_turnaround" backup_dist="0.4" backup_speed="0.2"/> -->
          <Wait wait_duration="2" server_timeout="5000"/>
          <!-- AckermannTurnAround: dùng Spin action interface, spin_dist = góc xoay mục tiêu (rad)
               3.14 rad = 180° đủ để thực hiện turnaround, plugin tự dừng sớm nếu đã đủ góc -->
          <Spin name="AckermannTurnAround" server_name="ackermann_turnaround" spin_dist="3.14" time_allowance="60.0" server_timeout="5000"/>
          <BackUp backup_dist="0.60" backup_speed="0.3" time_allowance="60.0" server_timeout="5000"/>
        </RoundRobin>
      </ReactiveFallback>
      
    </RecoveryNode>
  </BehaviorTree>
</root> 
